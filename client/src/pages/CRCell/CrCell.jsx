import React from "react";
import classes from "./CrCell.module.css";

const CrCell = props => {
  return (
    <section className={classes.main}>
      <h1>CR Cell</h1>
    </section>
  );
};

export default CrCell;
